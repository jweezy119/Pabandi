/**
 * 30+ realistic project templates for seeding the gig board.
 * Each project has real-world detail: title, description, skills, budget, milestones.
 */
export declare const REAL_PROJECTS: {
    title: string;
    description: string;
    category: string;
    requiredSkills: string[];
    budgetUsd: number;
    estimatedHours: number;
    demandGrowthPct: number;
    milestones: {
        name: string;
        pct: number;
    }[];
}[];
//# sourceMappingURL=realProjects.d.ts.map